# 高纳AI 项目总览

## 定位

面向工业 AOI 的少样本缺陷检测数据闭环平台。支持三种路线：
- 人工标注 + 监督训练
- AI 预标注 + 人工审核
- OK 样本异常检测

## 技术栈

| 层 | 技术 |
|---|---|
| 前端 | React 18 + TypeScript + Vite + Tailwind CSS + Fabric.js + Recharts |
| 后端 | Python FastAPI + SQLModel |
| 数据库 | SQLite (MVP) / PostgreSQL (生产) |
| 认证 | JWT |
| AI | OpenCV + sklearn (CPU) / YOLO + PyTorch (可选) |

## 配置项

`.env`:

| 变量 | 说明 | 默认值 |
|---|---|---|
| APP_NAME | 软件名称 | 高纳AI |
| COMPANY_NAME | 公司名称 | 高纳科技 |
| THEME | 主题 (light/dark/industrial) | light |

## 快速启动

```bash
# 后端
cd backend && python seed.py && uvicorn app.main:app --reload --port 8000

# 前端
cd frontend && npm run dev
```

## 目录结构

```
hetianxia/
├── backend/
│   ├── app/
│   │   ├── ai/          # AI 模块 (prelabel/training/eval/anomaly)
│   │   ├── core/        # 配置、安全、依赖注入、守卫、限流
│   │   ├── db/          # 数据库连接
│   │   ├── models/      # 14 个数据模型
│   │   ├── routers/     # 16 个路由模块 (30+ API)
│   │   ├── schemas/     # Pydantic 请求/响应
│   │   └── services/    # 14 个业务服务
│   ├── seed.py          # 演示数据
│   └── generate_test_images.py
├── frontend/
│   └── src/
│       ├── api/         # API 客户端 (7 模块)
│       ├── app/         # 路由、布局、主题
│       ├── features/    # 18 个功能页面
│       ├── stores/      # Zustand 状态
│       └── types/       # TypeScript 类型
├── docs/
│   └── vault/           # Obsidian 文档库
├── ops/
│   └── deploy.md        # 部署 SOP
├── docker-compose.yml
└── .env
```
